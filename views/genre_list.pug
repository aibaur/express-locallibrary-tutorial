extends layout

block content
  
  h3.text-primary #{title}:

  if genre_list.length
    ul
      each genre in genre_list
        li
          a(href=genre.url) #{genre.name}
        
  else
   p #[strong There are no genres.]
